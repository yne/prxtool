language: c
compiler: gcc
script:$CC -std=c99 -coverage prxtool.c -o prxtool -DVERSION=\"$(git describe --dirty --always --tags)\"
before_install:
  - pip install --user codecov
after_success:
  - codecov